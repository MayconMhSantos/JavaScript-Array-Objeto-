let nomes = ['maira', 'jose', 'pedro', 'joao']

// ForEath e um metodo existente no JavaScript
nomes.forEach(element => { // o Proprio metodo forEath criou a variavel elemento...
//... para receber cada elemento do array. (element = parametro da metodo).
    console.log('O nome e: ' + element);
    // o metodo ForEath vai executar cada elemento por vez imprimindo um a um.
});
